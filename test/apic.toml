title = "hello world"

# [rules.url_length]
# disable = true
#
[rules.url_similiarity_check.options]
weight = 0.90

[plugins.rules.test_plugin]
file = "./test/test_plugin.js"

[plugins.rules.test_plugin.options]
test_data = "hello"

[rules.url_length.options]
weight = 8

[rules.url_plural_checker.options]
blacklist_paths = ["/pet/findByStatuses"]
base_urls = ["/api/v1"]

# [rules.status_code_check.options]
# allowed_status_codes = [200, 201]
[rules.url_case_checker.options]
base_urls = ["/api/v1"]
